# 3D enclosure design

Drop your CAD renders/exports here, e.g.:
- `enclosure-render-front.png`
- `enclosure-render-exploded.png`

The repo root `README.md` links to these two filenames — add more and
extend the table there if you have additional angles.
